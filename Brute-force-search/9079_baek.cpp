#include<bits/stdc++.h>
using namespace std;

string c[3];

int main(void){
	ios::sync_with_stdio(0);
	cin.tie(0);
	int t;
	cin >> t;
	
	while(t--){
		for(int i = 0;i < 3;i++)
			cin >> c[i];
		
		
	}
	return 0;
}
